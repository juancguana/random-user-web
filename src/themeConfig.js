import { createMuiTheme  } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#00ff',
      light: '#00ffaa'
    },
    secondary: {
      main: '#ff00'
    }
  }
})

export default theme